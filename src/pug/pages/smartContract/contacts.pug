section.contacts
  .contacts__left
    h2.subPage__title.contacts__title Run your on-chain operations like&nbsp;a&nbsp;product, not a project
    p.contacts__text Ship automation with guarantees—no&nbsp;servers, no crons, no firefighting.
    a.btn.btn_primary(href=process.env.LINK_CONTACT_US target="_blank")
      | Talk to Sales
  .contacts__right
    h3.subPage__title.contacts__title Prefer email?
      br
      | contact@dittonetwork.io
    p.contacts__text Looking for vault rebalancing or fee routing at scale? Ask about Ditto Modules and SDK.
    a.btn_ditto(href=process.env.LINK_CONTACT_US target="_blank")
      +icon('ditto')
